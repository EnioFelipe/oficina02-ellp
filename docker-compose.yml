services:
  mongodb:
    image: mongo:7
    container_name: oficina02-mongodb
    environment:
      MONGO_INITDB_DATABASE: oficina02
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:

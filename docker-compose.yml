services:
  mongo:
    image: mongo:7
    container_name: oficina02-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - oficina02-mongo-data:/data/db

volumes:
  oficina02-mongo-data:
